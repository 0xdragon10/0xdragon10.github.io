title: 0xdragon
description: >-
  Offensive security researcher focused on binary exploitation,
  reverse engineering, and red team operations.
url: "https://0xdragon10.github.io"

markdown: kramdown
permalink: /posts/:title/

# Only plugins on GitHub Pages' native supported list —
# no custom build step / GitHub Actions required.
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Files/folders Jekyll should not try to process or publish
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
